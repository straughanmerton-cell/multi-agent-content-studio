name = "multi-agent-content-proxy"
main = "src/index.js"
compatibility_date = "2025-01-01"

# 免费套餐额度：每天 10 万次请求，个人使用完全够用。
# 变量请在 Cloudflare 控制台配置为 Secret，不要写进本文件后提交到仓库。
#
# [vars]
# ALLOWED_ORIGIN = "https://<你的用户名>.github.io"
# UPSTREAM_BASE = "https://api.deepseek.com/v1"

[observability]
enabled = true
